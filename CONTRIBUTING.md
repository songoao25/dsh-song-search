# Contributing

1. Create a focused branch.
2. Keep changes small and user-facing.
3. Run `npm test` and `npm run build`.
4. Do not commit API keys, local profile files, or private paths.
5. Use a Conventional Commit message such as `fix: improve search settings`.

Pull requests should explain the motivation, the user-visible change, and the tests run.
